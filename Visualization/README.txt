The tableau file connects to a SQL Database with the BAK file attached. The stored procedure does not have the REST ENDPOINT to the ML Jupyter Notebook yet to receive the produced data set.
The tableau file is meant to visualize what it might look like when receiving the data from the SQL Stored Procedure.

Tableau Parameters in Report -> SQL Stored Procedure -> AWS ML EndPoint -> Returns JSON Data -> SQL Stored Procedure Consumes JSON to Tabular Format -> Tableau Reloads the Data
